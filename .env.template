# =======================================================
# === Altrp Boilerplate Environment Variables Template
# =======================================================
# --- COPY THIS FILE TO .env AND FILL IN THE VALUES ---

# --- Project & Domain Settings ---
# Used by Traefik to create local domains
PROJECT_DOMAIN=altrp.localhost

# --- PostgreSQL Database ---
# Used by Payload, n8n, Metabase, etc.
POSTGRES_USER=altrp
POSTGRES_DB=altrp
POSTGRES_PASSWORD={GENERATE_POSTGRES_PASSWORD} # openssl rand -hex 24

# --- Payload CMS ---
# Connection string for Docker. DO NOT CHANGE 'postgres', it's the service name.
DATABASE_URI=postgres://altrp:{GENERATE_POSTGRES_PASSWORD}@postgres:5432/altrp
PAYLOAD_SECRET={GENERATE_PAYLOAD_SECRET} # openssl rand -hex 32

# --- n8n Automation ---
N8N_ENCRYPTION_KEY={GENERATE_N8N_ENCRYPTION_KEY} # openssl rand -hex 32

# --- AI Service (Mastra) ---
# Add any secrets for your AI service here
MASTRA_API_KEY={GENERATE_MASTRA_API_KEY}