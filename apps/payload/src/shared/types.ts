// Shared TypeScript types for Payload CMS

export interface User {
  id: string
  email: string
  // Add other user properties as needed
}

export interface SharedConfig {
  // Add shared configuration types
}